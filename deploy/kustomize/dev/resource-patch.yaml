# Dev: minimal resources
apiVersion: apps/v1
kind: Deployment
metadata:
  name: distllm-operator
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: operator
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
