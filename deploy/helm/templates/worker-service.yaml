{{- range $pool := .Values.nodePools }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "distllm.fullname" $ }}-worker-{{ $pool.name }}
  labels:
    {{- include "distllm.labels" $ | nindent 4 }}
    {{- include "distllm.workerLabels" (dict "root" $ "pool" $pool) | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "distllm.workerLabels" (dict "root" $ "pool" $pool) | nindent 4 }}
{{- end }}
