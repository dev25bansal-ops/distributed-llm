apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributed-llm.fullname" . }}-coordinator
  labels:
    {{- include "distributed-llm.labels" . | nindent 4 }}
    {{- include "distributed-llm.coordinatorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.coordinator.port }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.coordinator.grpcPort }}
      targetPort: grpc
      protocol: TCP
    - name: metrics
      port: {{ .Values.coordinator.metricsPort }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "distributed-llm.coordinatorLabels" . | nindent 4 }}
