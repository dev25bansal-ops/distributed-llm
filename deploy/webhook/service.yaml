apiVersion: v1
kind: Service
metadata:
  name: distllm-webhook
  namespace: distllm-system
  labels:
    app: distllm-webhook
    app.kubernetes.io/name: distributed-llm
spec:
  ports:
    - name: https
      port: 443
      targetPort: https
      protocol: TCP
  selector:
    app: distllm-webhook
