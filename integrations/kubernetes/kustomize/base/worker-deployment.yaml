apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  labels:
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: distllm-worker
      initContainers:
        - name: wait-for-redis
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z distllm-redis 6379; do echo waiting for redis; sleep 2; done
      containers:
        - name: worker
          image: distributed-llm:latest
          imagePullPolicy: IfNotPresent
          args:
            - worker
          ports:
            - containerPort: 9090
              name: metrics
          env:
            - name: DISTLLM_CONFIG
              value: /etc/distllm/distllm.yaml
            - name: DISTLLM_LOG_LEVEL
              value: info
            - name: DISTLLM_WORKER_PORT
              value: "9090"
          volumeMounts:
            - name: config
              mountPath: /etc/distllm
            - name: models
              mountPath: /data/models
            - name: cache
              mountPath: /data/cache
          resources:
            requests:
              cpu: 4
              memory: 16Gi
            limits:
              cpu: 16
              memory: 64Gi
              nvidia.com/gpu: 1
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: distllm-config
        - name: models
          persistentVolumeClaim:
            claimName: distllm-models
        - name: cache
          persistentVolumeClaim:
            claimName: distllm-cache
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: accelerator
                    operator: In
                    values:
                      - nvidia-tesla-a100
                      - nvidia-a100-80gb
